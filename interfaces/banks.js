
/* const { PutBanks } = require('../repositories/banks')
const { Put_Banks } = require('../controllers/banks')

const BanksRepo = (req, res) => {
    var id;
    var code;
    var description;
    const Put_Banks_C = await Put_Banks(id, code, description);
    res.json(Put_Banks_C);
}

const BanksControll = (req, res) => {
    var f_id;
    var f_code;
    var f_description;
    const Put_Banks_R = await PutBanks(f_id, f_code, f_description, datenow);
    res.json(Put_Banks_R);
} */